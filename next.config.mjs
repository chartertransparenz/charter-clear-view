/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['[IHRE-DOMAIN]'],
  },
  // Experimental app directory is now stable in Next.js 14
}

export default nextConfig;